# Normalise line endings to LF in the repo; check out native on each platform.
* text=auto eol=lf

# Binary assets — never normalise.
*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
*.loga binary
*.nmea -text
